# Build artifacts (regenerated by build process)
examples/*/.bin/
examples/*/.build/

# Go workspace (local development)
go.work
go.work.sum

# goup-util binaries and bootstrap scripts (build with 'go run . self build')
goup-util-*
macos-bootstrap.sh
windows-bootstrap.ps1

# Source code references (for reading dependencies)
# Keep Taskfile but ignore cloned repos
.src/*
!.src/Taskfile.yml

# Build and distribution directories
.build/
.dist/
artifacts/

# Standalone goup-util binary
goup-util
tools/
