## goup-util utm install

Install UTM app or download VM ISO

### Synopsis

Install the UTM application or download a VM ISO from the gallery.

Without arguments, installs the UTM application.
With a VM key, downloads the ISO for that VM.

Examples:
  # Install UTM app
  goup-util utm install

  # Download Windows 11 ISO
  goup-util utm install windows-11-arm

  # Force reinstall UTM
  goup-util utm install --force

```
goup-util utm install [vm-key] [flags]
```

### Options

```
      --force   Force reinstall/redownload
  -h, --help    help for install
```

### SEE ALSO

* [goup-util utm](goup-util_utm.md)	 - Control UTM virtual machines

###### Auto generated by spf13/cobra on 5-Feb-2026
