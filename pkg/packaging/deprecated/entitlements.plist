<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Hardened Runtime -->
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>

	<!-- Network (if needed for downloading SDKs) -->
	<key>com.apple.security.network.client</key>
	<true/>
	<key>com.apple.security.network.server</key>
	<true/>

	<!-- File Access -->
	<key>com.apple.security.files.user-selected.read-write</key>
	<true/>
	<key>com.apple.security.files.downloads.read-write</key>
	<true/>

	<!-- NOTE: Screen Recording Permission -->
	<!-- There is NO entitlement for screen recording in third-party apps -->
	<!-- The private entitlements like com.apple.private.screencapture.allow -->
	<!-- are only available to Apple's own applications -->
	<!-- -->
	<!-- Apps must: -->
	<!-- 1. Be properly code-signed with a Developer ID or Apple ID -->
	<!-- 2. Trigger screen recording functionality (which shows the permission dialog) -->
	<!-- 3. Have the user manually grant permission in System Settings -->
</dict>
</plist>
